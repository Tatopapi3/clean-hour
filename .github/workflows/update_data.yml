name: Update Grid Data (all regions)

on:
  schedule:
    - cron: '0 * * * *'   # every hour at :00
  workflow_dispatch:        # allow manual trigger from GitHub UI

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch EIA data (all regions)
        env:
          EIA_API_KEY: ${{ secrets.EIA_API_KEY }}
        run: |
          python fetch_data.py --region CAL   # California CAISO
          python fetch_data.py --region TEX   # Texas ERCOT
          python fetch_data.py --region NYIS  # New York ISO
          python fetch_data.py --region MIDA  # PJM Mid-Atlantic

      - name: Run analysis (all regions)
        run: |
          python analyze.py --region CAL
          python analyze.py --region TEX
          python analyze.py --region NYIS
          python analyze.py --region MIDA

      - name: Commit updated data
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git diff --cached --quiet || git commit -m "chore: update grid data for all regions $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
